# Netsapify.com
1. npx degit the repo (or copy the 12 snippets above). 2. Drop in your bridge API key and admin wallet. 3. git push → Netlify “New site from Git” → build succeeds. 4. Open app → Phantom pops up → real SOL swap → real BTC landing in any wallet you choose. No fake signatures, no RPC red-flags, no Netlify bans. Ship it 🚀
